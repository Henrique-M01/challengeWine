import styled from 'styled-components';

const BigPrice = styled.span`
  font-size: large;
  color: #B6116E;
  font-family: 'Lato';
  font-style: normal;
  font-weight: 700;
  line-height: 27px;

  span {
    font-family: 'Lato';
    font-style: normal;
    font-weight: 700;
    font-size: 11px;
    line-height: 10px;
  }
`;

export default BigPrice;
