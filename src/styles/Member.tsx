import styled from 'styled-components';

const Member = styled.span`
  font-family: 'Lato';
  font-style: normal;
  font-weight: 700;
  font-size: 11px;
  line-height: 10px;
`;

export default Member;
