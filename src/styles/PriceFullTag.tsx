import styled from 'styled-components';

const PriceFullTag = styled.span`
  color: #888888;
  font-family: 'Lato';
  font-style: normal;
  font-weight: 700;
  font-size: 11px;
  line-height: 16px;
  text-align: center;
  text-decoration-line: line-through;
`;

export default PriceFullTag;
